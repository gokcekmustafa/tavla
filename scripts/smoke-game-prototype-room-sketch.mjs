import { readFileSync } from "node:fs";
import { resolve } from "node:path";

const appPath = resolve("src", "App.tsx");
const cssPath = resolve("src", "App.css");
const appSource = readFileSync(appPath, "utf8");
const cssSource = readFileSync(cssPath, "utf8");

function hasApp(text) {
  return appSource.includes(text);
}

function hasCss(text) {
  return cssSource.includes(text);
}

const checks = [
  {
    label: "101 oda taslagi local state ile ac/kapatiliyor",
    test: () =>
      hasApp("const OKEY_PROTOTYPE_TOTAL_TILE_COUNT = 106;")
      && hasApp("const [okeyPrototypeRoomSketchOpen, setOkeyPrototypeRoomSketchOpen] = useState(false);")
      && hasApp("const [okeyPrototypeRoomFilter, setOkeyPrototypeRoomFilter] = useState<\"all\" | \"fast\" | \"busy\">(\"all\");")
      && hasApp("const [okeyPrototypeSelectedRoomId, setOkeyPrototypeSelectedRoomId] = useState<string>(OKEY_PROTOTYPE_ROOMS[0]?.id ?? \"\");")
      && hasApp("const [okeyPrototypeTableFilter, setOkeyPrototypeTableFilter] = useState<\"all\" | \"active\" | \"waiting\">(\"all\");")
      && hasApp("const [okeyPrototypeOnlyWithFreeSeats, setOkeyPrototypeOnlyWithFreeSeats] = useState(false);")
      && hasApp("const [okeyPrototypeTableSearch, setOkeyPrototypeTableSearch] = useState(\"\");")
      && hasApp("const [okeyPrototypeTableSort, setOkeyPrototypeTableSort] = useState<\"tableNo\" | \"occupancy\" | \"status\">(\"tableNo\");")
      && hasApp("const [okeyPrototypeSelectedTableId, setOkeyPrototypeSelectedTableId] = useState(\"\");")
      && hasApp("const [okeyPrototypeSeatDraft, setOkeyPrototypeSeatDraft] = useState(1);")
      && hasApp("const [okeyPrototypeSeatReservation, setOkeyPrototypeSeatReservation] = useState<{ tableId: string; seatNo: number } | null>(null);")
      && hasApp("const [okeyPrototypeTurnSeat, setOkeyPrototypeTurnSeat] = useState<1 | 2 | 3 | 4>(1);")
      && hasApp("const [okeyPrototypeTurnRound, setOkeyPrototypeTurnRound] = useState(1);")
      && hasApp("const [okeyPrototypeTurnPhase, setOkeyPrototypeTurnPhase] = useState<OkeyPrototypeTurnPhase>(\"draw\");")
      && hasApp("const [okeyPrototypeTurnLockedAfterMeld, setOkeyPrototypeTurnLockedAfterMeld] = useState(false);")
      && hasApp("const [okeyPrototypeSeatOpenedState, setOkeyPrototypeSeatOpenedState] = useState<OkeyPrototypeSeatOpenedState>(() => createDefaultOkeyPrototypeSeatOpenedState());")
      && hasApp("const [okeyPrototypeRackState, setOkeyPrototypeRackState] = useState<OkeyPrototypeRackState>(() => createOkeyPrototypeRackState());")
      && hasApp("const [okeyPrototypeDiscardPile, setOkeyPrototypeDiscardPile] = useState<OkeyPrototypeDiscardEntry[]>([]);")
      && hasApp("const [okeyPrototypeDiscardDraftTileId, setOkeyPrototypeDiscardDraftTileId] = useState(\"\");")
      && hasApp("const [okeyPrototypeMeldDraftTileIds, setOkeyPrototypeMeldDraftTileIds] = useState<string[]>([]);")
      && hasApp("const [okeyPrototypeOpenedMelds, setOkeyPrototypeOpenedMelds] = useState<OkeyPrototypeMeldEntry[]>([]);")
      && hasApp("const [okeyPrototypeActionLog, setOkeyPrototypeActionLog] = useState<Array<{ id: string; at: number; text: string }>>([]);")
      && hasApp("const okeyPrototypeSelectedTableIndex = useMemo(() => {")
      && hasApp("const okeyPrototypeSelectedTablePosition = okeyPrototypeSelectedTableIndex >= 0 ? okeyPrototypeSelectedTableIndex + 1 : 0;")
      && hasApp("const okeyPrototypeRandomFreeSeatTableCandidates = useMemo(() => {")
      && hasApp("const okeyPrototypeCanAdvanceTurn = Boolean(okeyPrototypeSeatReservation && okeyPrototypeJoinedTable);")
      && hasApp("const okeyPrototypeNextTurnSeat = okeyPrototypeTurnSeat === 4 ? 1 : (okeyPrototypeTurnSeat + 1) as 1 | 2 | 3 | 4;")
      && hasApp("const okeyPrototypeSeatNoForRack = useMemo(() => {")
      && hasApp("const okeyPrototypeSeatRackTiles = useMemo(() => {")
      && hasApp("const okeyPrototypeDiscardDraftTile = useMemo(() => {")
      && hasApp("const okeyPrototypeMeldDraftTiles = useMemo(() => {")
      && hasApp("const okeyPrototypeMeldDraftValidation = useMemo(() => {")
      && hasApp("const okeyPrototypeRackSeatSummaries = useMemo(() => {")
      && hasApp("const okeyPrototypeRackTileCount = okeyPrototypeRackSeatSummaries.reduce((sum, item) => sum + item.tileCount, 0);")
      && hasApp("const okeyPrototypeLastDiscard = okeyPrototypeDiscardPile[0] ?? null;")
      && hasApp("const okeyPrototypeDiscardPreview = okeyPrototypeDiscardPile.slice(0, 8);")
      && hasApp("const okeyPrototypeOpenedMeldPreview = okeyPrototypeOpenedMelds.slice(0, 8);")
      && hasApp("const okeyPrototypeDrawPileRemaining = Math.max(0, OKEY_PROTOTYPE_TOTAL_TILE_COUNT - (okeyPrototypeRackTileCount + okeyPrototypeDiscardPile.length));")
      && hasApp("const okeyPrototypeHasTableFilters = Boolean(")
      && hasApp("const okeyPrototypeCurrentSeatOpened = okeyPrototypeSeatOpenedState[okeyPrototypeTurnSeat as OkeyPrototypeSeatNo] ?? false;")
      && hasApp("const okeyPrototypeSeatOpenedSummary = OKEY_PROTOTYPE_SEATS.map((seatNo) => ({")
      && hasApp("const okeyPrototypeCanDrawTile = okeyPrototypeCanAdvanceTurn")
      && hasApp("&& okeyPrototypeDrawPileRemaining > 0;")
      && hasApp("const okeyPrototypeCanDiscardTile = okeyPrototypeCanAdvanceTurn && okeyPrototypeTurnPhase === \"discard\" && okeyPrototypeSeatRackTiles.length > 0;")
      && hasApp("const okeyPrototypeActiveFilters = useMemo(() => {")
      && hasApp("function appendOkeyPrototypeAction(rawText: string) {")
      && hasApp("function resetOkeyPrototypeTableFilters() {")
      && hasApp("function clearOkeyPrototypeFilterChip(filterKey: string, label: string) {")
      && hasApp("function moveOkeyPrototypeTableSelection(step: number) {")
      && hasApp("function jumpOkeyPrototypeTableSelection(target: \"first\" | \"last\") {")
      && hasApp("function pickRandomOkeyPrototypeTable() {")
      && hasApp("function pickRandomOkeyPrototypeFreeSeatTable() {")
      && hasApp("function selectFirstOkeyPrototypeSearchResult() {")
      && hasApp("function buildOkeyPrototypeDrawTile(seatNo: OkeyPrototypeSeatNo, currentLength: number): OkeyPrototypeTile {")
      && hasApp("function moveOkeyPrototypeTurnToNextSeat() {")
      && hasApp("function selectOkeyPrototypeDiscardDraft(tileId: string) {")
      && hasApp("function toggleOkeyPrototypeMeldDraftTile(tileId: string) {")
      && hasApp("function clearOkeyPrototypeMeldDraft() {")
      && hasApp("function openOkeyPrototypeMeld() {")
      && hasApp("El acildi: Koltuk")
      && hasApp("Gecersiz per: Turu bitirmek icin en az 1 tas elde kalmali.")
      && hasApp("function drawOkeyPrototypeTile() {")
      && hasApp("Kapali destede tas kalmadi.")
      && hasApp("Kapali deste bitti: Son tas cekildi.")
      && hasApp("function discardOkeyPrototypeTile() {")
      && hasApp("function advanceOkeyPrototypeTurn() {")
      && hasApp("Tur kilidi aktif: Per actiktan sonra tasi atmadan tur devredemezsin.")
      && hasApp("function resetOkeyPrototypeTurn() {")
      && hasApp("function refreshOkeyPrototypeRackState() {")
      && hasApp("onClick={() => setOkeyPrototypeRoomSketchOpen((prev) => !prev)}")
      && hasApp("{okeyPrototypeRoomSketchOpen ? \"101 Oda Taslagini Kapat\" : \"101 Oda Taslagini Ac\"}"),
  },
  {
    label: "101 oda taslagi kartlari render ediliyor",
    test: () =>
      hasApp("const OKEY_PROTOTYPE_ROOMS = [")
      && hasApp("className=\"my-game-coming-room-shell\"")
      && hasApp("className=\"my-game-coming-room-filters\"")
      && hasApp("className={`my-game-coming-room-filter ${okeyPrototypeRoomFilter === \"all\" ? \"active\" : \"\"}`}")
      && hasApp("className=\"my-game-coming-room-selected\"")
      && hasApp("className=\"my-game-coming-room-grid\"")
      && hasApp("okeyPrototypeFilteredRooms.map((room) => (")
      && hasApp("className={`my-game-coming-room-card ${okeyPrototypeSelectedRoomId === room.id ? \"active\" : \"\"}`}")
      && hasApp("appendOkeyPrototypeAction(`Oda secildi: ${room.name}`);")
      && hasApp("Gorunen Oda: {okeyPrototypeFilteredRooms.length} | Toplam Oyuncu: {okeyPrototypeFilteredPlayers}")
      && hasApp("const okeyPrototypeTableSketchRows = useMemo(() => {")
      && hasApp("className=\"my-game-coming-table-sketch\"")
      && hasApp("className=\"my-game-coming-table-sketch-filters\"")
      && hasApp("Masa | Filtre: {okeyPrototypeActiveFilters.length}")
      && hasApp("className={`my-game-coming-table-sketch-filter ${okeyPrototypeTableFilter === \"all\" ? \"active\" : \"\"}`}")
      && hasApp("aria-pressed={okeyPrototypeTableFilter === \"all\"}")
      && hasApp("title=\"Tum masa filtrelerini goster\"")
      && hasApp("aria-label=\"Tum masa filtrelerini goster\"")
      && hasApp("aria-pressed={okeyPrototypeTableFilter === \"active\"}")
      && hasApp("title=\"Sadece aktif masalari goster\"")
      && hasApp("aria-label=\"Sadece aktif masalari goster\"")
      && hasApp("aria-pressed={okeyPrototypeTableFilter === \"waiting\"}")
      && hasApp("title=\"Sadece bekleyen masalari goster\"")
      && hasApp("aria-label=\"Sadece bekleyen masalari goster\"")
      && hasApp("className={`my-game-coming-table-sketch-filter ${okeyPrototypeOnlyWithFreeSeats ? \"active\" : \"\"}`}")
      && hasApp("aria-pressed={okeyPrototypeOnlyWithFreeSeats}")
      && hasApp("title={okeyPrototypeOnlyWithFreeSeats ? \"Bos koltuk filtresini kapat\" : \"Bos koltuk filtresini ac\"}")
      && hasApp("aria-label={okeyPrototypeOnlyWithFreeSeats ? \"Bos koltuk filtresini kapat\" : \"Bos koltuk filtresini ac\"}")
      && hasApp("Bos Koltuk Var")
      && hasApp("className=\"my-game-coming-table-sketch-sort\"")
      && hasApp("className=\"my-game-coming-table-sketch-sort-select\"")
      && hasApp("title=\"Masa listesini siralar\"")
      && hasApp("aria-label=\"Masa listesi siralama secimi\"")
      && hasApp("aria-controls=\"okey-prototype-table-sketch-grid\"")
      && hasApp("const okeyPrototypeVisibleTableSketchRows = useMemo(() => {")
      && hasApp("className=\"my-game-coming-table-sketch-search\"")
      && hasApp("className=\"my-game-coming-table-sketch-search-row\"")
      && hasApp("className=\"my-game-coming-table-sketch-search-input\"")
      && hasApp("pattern=\"[0-9]*\"")
      && hasApp("enterKeyHint=\"search\"")
      && hasApp("autoComplete=\"off\"")
      && hasApp("spellCheck={false}")
      && hasApp("maxLength={2}")
      && hasApp("title=\"Masa numarasina gore arama yapar\"")
      && hasApp("aria-describedby=\"okey-prototype-table-search-meta\"")
      && hasApp("aria-controls=\"okey-prototype-table-sketch-grid\"")
      && hasApp("onKeyDown={(e) => {")
      && hasApp("if (e.key !== \"Enter\") return;")
      && hasApp("selectFirstOkeyPrototypeSearchResult();")
      && hasApp("className=\"my-action-btn soft my-game-coming-table-sketch-search-pick-btn\"")
      && hasApp("title=\"Arama sonucundaki ilk masayi secer\"")
      && hasApp("aria-label=\"Arama sonucundaki ilk masayi sec\"")
      && hasApp("Ilk Sonuc")
      && hasApp("Arama sonucu secildi:")
      && hasApp("className=\"my-action-btn soft my-game-coming-table-sketch-search-clear-btn\"")
      && hasApp("title=\"Masa arama kutusunu temizler\"")
      && hasApp("aria-label=\"Masa aramasini temizle\"")
      && hasApp("Masa aramasi temizlendi.")
      && hasApp("Temizle")
      && hasApp("className=\"my-game-coming-table-sketch-search-meta\"")
      && hasApp("id=\"okey-prototype-table-search-meta\"")
      && hasApp("aria-live=\"polite\"")
      && hasApp("role=\"status\"")
      && hasApp("id=\"okey-prototype-table-search-meta\" className=\"my-game-coming-table-sketch-search-meta\" aria-live=\"polite\" role=\"status\" aria-atomic=\"true\"")
      && hasApp("Sonuc: {okeyPrototypeVisibleTableSketchRows.length} masa | Enter: Ilk Sonuc")
      && hasApp("className=\"my-action-btn soft my-game-coming-table-sketch-reset-btn\"")
      && hasApp("title=\"Tum masa filtrelerini temizler\"")
      && hasApp("aria-label={`Filtreleri sifirla. Aktif filtre: ${okeyPrototypeActiveFilters.length}`}")
      && hasApp("Filtreleri Sifirla")
      && hasApp("Filtreleri Sifirla ({okeyPrototypeActiveFilters.length})")
      && hasApp("className=\"my-game-coming-table-sketch-active-filters\"")
      && hasApp("className=\"my-game-coming-table-sketch-filter-count\"")
      && hasApp("className=\"my-game-coming-table-sketch-filter-count\" aria-live=\"polite\"")
      && hasApp("className=\"my-game-coming-table-sketch-filter-count\" aria-live=\"polite\" role=\"status\"")
      && hasApp("className=\"my-game-coming-table-sketch-filter-count\" aria-live=\"polite\" role=\"status\" aria-atomic=\"true\"")
      && hasApp("Aktif filtreler: {okeyPrototypeActiveFilters.length}")
      && hasApp("className=\"my-game-coming-table-sketch-filter-chip-btn\"")
      && hasApp("title={`${filter.label} filtresini kaldir`}")
      && hasApp("className=\"my-game-coming-table-sketch-filter-empty\"")
      && hasApp("className=\"my-game-coming-table-sketch-filter-empty\" aria-live=\"polite\" role=\"status\"")
      && hasApp("className=\"my-game-coming-table-sketch-filter-empty\" aria-live=\"polite\" role=\"status\" aria-atomic=\"true\"")
      && hasApp("Aktif filtre yok.")
      && hasApp("onClick={() => clearOkeyPrototypeFilterChip(filter.key, filter.label)}")
      && hasApp("className=\"my-game-coming-table-sketch-selected\"")
      && hasApp("className=\"my-game-coming-table-sketch-selected\" aria-live=\"polite\" role=\"status\"")
      && hasApp("className=\"my-game-coming-table-sketch-selected\" aria-live=\"polite\" role=\"status\" aria-atomic=\"true\"")
      && hasApp("className=\"my-game-coming-table-sketch-nav\"")
      && hasApp("className=\"my-game-coming-table-sketch-nav-indicator\"")
      && hasApp("className=\"my-game-coming-table-sketch-nav-indicator\" aria-live=\"polite\" role=\"status\" aria-atomic=\"true\"")
      && hasApp("{okeyPrototypeSelectedTablePosition}/{okeyPrototypeVisibleTableSketchRows.length}")
      && hasApp("className=\"my-action-btn soft my-game-coming-table-sketch-nav-btn\"")
      && hasApp("onClick={() => jumpOkeyPrototypeTableSelection(\"first\")}")
      && hasApp("title=\"Ilk masaya git\"")
      && hasApp("aria-label=\"Ilk masaya git\"")
      && hasApp("onClick={() => moveOkeyPrototypeTableSelection(-1)}")
      && hasApp("title=\"Onceki masaya git\"")
      && hasApp("aria-label=\"Onceki masaya git\"")
      && hasApp("onClick={() => moveOkeyPrototypeTableSelection(1)}")
      && hasApp("title=\"Sonraki masaya git\"")
      && hasApp("aria-label=\"Sonraki masaya git\"")
      && hasApp("onClick={() => jumpOkeyPrototypeTableSelection(\"last\")}")
      && hasApp("title=\"Son masaya git\"")
      && hasApp("aria-label=\"Son masaya git\"")
      && hasApp("Ilk Masa")
      && hasApp("Onceki Masa")
      && hasApp("Sonraki Masa")
      && hasApp("onClick={pickRandomOkeyPrototypeTable}")
      && hasApp("title=\"Rastgele masa sec\"")
      && hasApp("aria-label=\"Rastgele masa sec\"")
      && hasApp("Rastgele Masa")
      && hasApp("onClick={pickRandomOkeyPrototypeFreeSeatTable}")
      && hasApp("title=\"Rastgele bos koltuklu masa sec\"")
      && hasApp("aria-label=\"Rastgele bos koltuklu masa sec\"")
      && hasApp("Rastgele Bos")
      && hasApp("Son Masa")
      && hasApp("const okeyPrototypeTableSummary = useMemo(() => {")
      && hasApp("const okeyPrototypeQuickJoinTable = useMemo(() => {")
      && hasApp("className=\"my-game-coming-table-sketch-summary\"")
      && hasApp("className=\"my-game-coming-table-sketch-summary-item\"")
      && hasApp("<span>Toplam Dolu</span>")
      && hasApp("<span>Toplam Bos</span>")
      && hasApp("className=\"my-game-coming-table-sketch-quick-join\"")
      && hasApp("Hizli Oneri: Masa {okeyPrototypeQuickJoinTable.tableNo}")
      && hasApp("appendOkeyPrototypeAction(`Hizli oneriden masa secildi: ${okeyPrototypeQuickJoinTable.tableNo}`);")
      && hasApp("Onerilen Masayi Sec")
      && hasApp("const okeyPrototypeSelectedTableSeats = useMemo(() => {")
      && hasApp("const okeyPrototypeAvailableSeatNos = useMemo(() => {")
      && hasApp("className=\"my-game-coming-table-seat-row\"")
      && hasApp("className={`my-game-coming-table-seat ${seat.occupied ? \"occupied\" : \"empty\"} ${!seat.occupied && okeyPrototypeSeatDraft === seat.seatNo ? \"draft\" : \"\"}`}")
      && hasApp("className=\"my-game-coming-table-seat-actions\"")
      && hasApp("className=\"my-game-coming-table-seat-chip-row\"")
      && hasApp("className={`my-game-coming-table-seat-chip ${okeyPrototypeSeatDraft === seatNo ? \"active\" : \"\"}`}")
      && hasApp("Masaya Otur (Prototip)")
      && hasApp("Masadan Ayril (Prototip)")
      && hasApp("aria-describedby=\"okey-prototype-seat-status\"")
      && hasApp("id=\"okey-prototype-seat-status\"")
      && hasApp("className=\"my-game-coming-table-seat-status\"")
      && hasApp("role=\"status\"")
      && hasApp("aria-live=\"polite\"")
      && hasApp("aria-atomic=\"true\"")
      && hasApp("className=\"my-game-coming-prototype-turn\"")
      && hasApp("Tur Yonetimi (Prototip)")
      && hasApp("Faz:")
      && hasApp("className=\"my-game-coming-prototype-turn-pile-status\"")
      && hasApp("Kapali Deste:")
      && hasApp("className=\"my-game-coming-prototype-turn-open-status\"")
      && hasApp("El Durumu:")
      && hasApp("id=\"okey-prototype-turn-status\"")
      && hasApp("className=\"my-game-coming-prototype-turn-status\"")
      && hasApp("className=\"my-game-coming-prototype-turn-lock\"")
      && hasApp("Tur kilidi aktif: Per actin. Turu devretmek icin once tas atmalisin.")
      && hasApp("Tas Cek (Prototip)")
      && hasApp("Tas At (Prototip)")
      && hasApp("Turu Ilerle (Prototip)")
      && hasApp("Turu Sifirla (Prototip)")
      && hasApp("Tur manuel ilerletildi:")
      && hasApp("Tur sifirlandi:")
      && hasApp("Tas cekildi:")
      && hasApp("Tas atildi:")
      && hasApp("className=\"my-game-coming-prototype-rack\"")
      && hasApp("Tas Dizilimi (Prototip)")
      && hasApp("className=\"my-game-coming-prototype-rack-seat-summary\"")
      && hasApp("className=\"my-game-coming-prototype-rack-tiles\"")
      && hasApp("className={`my-game-coming-prototype-rack-tile tile-${tile.color} ${okeyPrototypeDiscardDraftTileId === tile.id ? \"discard-selected\" : \"\"} ${okeyPrototypeMeldDraftTileIds.includes(tile.id) ? \"meld-selected\" : \"\"}`}")
      && hasApp("Atilacak tas secildi:")
      && hasApp("className=\"my-game-coming-prototype-rack-discard-status\"")
      && hasApp("Atilacak tas:")
      && hasApp("Atilacak tasi rafdan sec.")
      && hasApp("className=\"my-game-coming-prototype-meld-draft-status\"")
      && hasApp("Per taslagi:")
      && hasApp("Per Ac (Prototip)")
      && hasApp("Per Taslagini Temizle")
      && hasApp("Per acildi:")
      && hasApp("Taslari Yeniden Dagit (Prototip)")
      && hasApp("Tas dizilimi yenilendi.")
      && hasApp("className=\"my-game-coming-prototype-melds\"")
      && hasApp("Acilan Perler (Prototip)")
      && hasApp("className=\"my-game-coming-prototype-melds-open-summary\"")
      && hasApp("className=\"my-game-coming-prototype-melds-list\"")
      && hasApp("className=\"my-game-coming-prototype-meld-item\"")
      && hasApp("className=\"my-game-coming-prototype-meld-item-tiles\"")
      && hasApp("Henuz acilan per yok.")
      && hasApp("className=\"my-game-coming-prototype-discard\"")
      && hasApp("Orta Alan (Prototip)")
      && hasApp("className=\"my-game-coming-prototype-discard-status\"")
      && hasApp("Son atilan:")
      && hasApp("Henuz orta alana atilan tas yok.")
      && hasApp("className=\"my-game-coming-prototype-discard-tiles\"")
      && hasApp("className=\"my-game-coming-prototype-discard-empty\"")
      && hasApp("className=\"my-game-coming-prototype-log\"")
      && hasApp("title=\"Prototip olay gecmisini temizler\"")
      && hasApp("aria-label=\"Prototip olay gecmisini temizle\"")
      && hasApp("className=\"my-game-coming-prototype-log-list\"")
      && hasApp("className=\"my-game-coming-prototype-log-list\" role=\"log\" aria-live=\"polite\" aria-relevant=\"additions text\"")
      && hasApp("className=\"my-game-coming-prototype-log-entry\"")
      && hasApp("Henuz kayitli prototip aksiyonu yok.")
      && hasApp("className=\"my-game-coming-table-sketch-grid\"")
      && hasApp("id=\"okey-prototype-table-sketch-grid\"")
      && hasApp("role=\"region\"")
      && hasApp("aria-label={`Masa kart listesi. Gorunen masa: ${okeyPrototypeVisibleTableSketchRows.length}`}")
      && hasApp("className=\"my-game-coming-table-sketch-empty-state\"")
      && hasApp("Aramaya uygun masa bulunamadi.")
      && hasApp("title=\"Filtreleri temizleyip masa listesini yeniler\"")
      && hasApp("aria-label=\"Filtreleri temizle ve tekrar dene\"")
      && hasApp("Filtreleri Temizle ve Tekrar Dene")
      && hasApp("okeyPrototypeVisibleTableSketchRows.map((row) => (")
      && hasApp("className={`my-game-coming-table-sketch-card ${row.active ? \"active\" : \"waiting\"} ${okeyPrototypeSelectedTableId === row.id ? \"selected\" : \"\"}`}")
      && hasApp("title={`Masa ${row.tableNo}, durum ${row.active ? \"aktif\" : \"bekliyor\"}, dolu koltuk ${row.seated}/4`}")
      && hasApp("aria-label={`Masa ${row.tableNo}, durum ${row.active ? \"aktif\" : \"bekliyor\"}, dolu koltuk ${row.seated}/4`}")
      && hasApp("aria-pressed={okeyPrototypeSelectedTableId === row.id}")
      && hasApp("appendOkeyPrototypeAction(`Masa secildi: ${row.tableNo}`);"),
  },
  {
    label: "101 oda taslagi css siniflari mevcut",
    test: () =>
      hasCss(".my-game-coming-room-shell {")
      && hasCss(".my-game-coming-room-head {")
      && hasCss(".my-game-coming-room-filters {")
      && hasCss(".my-game-coming-room-filter.active {")
      && hasCss(".my-game-coming-room-selected {")
      && hasCss(".my-game-coming-room-grid {")
      && hasCss(".my-game-coming-room-card {")
      && hasCss(".my-game-coming-room-card.active {")
      && hasCss(".my-game-coming-table-sketch {")
      && hasCss(".my-game-coming-table-sketch-filters {")
      && hasCss(".my-game-coming-table-sketch-filter.active {")
      && hasCss(".my-game-coming-table-sketch-sort {")
      && hasCss(".my-game-coming-table-sketch-sort-select {")
      && hasCss(".my-game-coming-table-sketch-search {")
      && hasCss(".my-game-coming-table-sketch-search-row {")
      && hasCss(".my-game-coming-table-sketch-search-input {")
      && hasCss(".my-game-coming-table-sketch-search-pick-btn {")
      && hasCss(".my-game-coming-table-sketch-search-clear-btn {")
      && hasCss(".my-game-coming-table-sketch-search-meta {")
      && hasCss(".my-game-coming-table-sketch-reset-btn {")
      && hasCss(".my-game-coming-table-sketch-active-filters {")
      && hasCss(".my-game-coming-table-sketch-filter-count {")
      && hasCss(".my-game-coming-table-sketch-filter-empty {")
      && hasCss(".my-game-coming-table-sketch-filter-chip-btn {")
      && hasCss(".my-game-coming-table-sketch-selected {")
      && hasCss(".my-game-coming-table-sketch-nav {")
      && hasCss(".my-game-coming-table-sketch-nav-indicator {")
      && hasCss(".my-game-coming-table-sketch-nav-btn {")
      && hasCss(".my-game-coming-table-sketch-summary {")
      && hasCss(".my-game-coming-table-sketch-summary-item {")
      && hasCss(".my-game-coming-table-sketch-quick-join {")
      && hasCss(".my-game-coming-table-seat-row {")
      && hasCss(".my-game-coming-table-seat.occupied {")
      && hasCss(".my-game-coming-table-seat.draft {")
      && hasCss(".my-game-coming-table-seat-actions {")
      && hasCss(".my-game-coming-table-seat-chip.active {")
      && hasCss(".my-game-coming-table-seat-status {")
      && hasCss(".my-game-coming-prototype-turn {")
      && hasCss(".my-game-coming-prototype-turn-head {")
      && hasCss(".my-game-coming-prototype-turn-pile-status {")
      && hasCss(".my-game-coming-prototype-turn-open-status {")
      && hasCss(".my-game-coming-prototype-turn-status {")
      && hasCss(".my-game-coming-prototype-turn-lock {")
      && hasCss(".my-game-coming-prototype-turn-actions {")
      && hasCss(".my-game-coming-prototype-rack {")
      && hasCss(".my-game-coming-prototype-rack-head {")
      && hasCss(".my-game-coming-prototype-rack-seat-summary {")
      && hasCss(".my-game-coming-prototype-rack-tiles {")
      && hasCss(".my-game-coming-prototype-rack-tile {")
      && hasCss(".my-game-coming-prototype-rack-tile.discard-selected {")
      && hasCss(".my-game-coming-prototype-rack-tile.meld-selected {")
      && hasCss(".my-game-coming-prototype-rack-tile.tile-kirmizi {")
      && hasCss(".my-game-coming-prototype-rack-tile.tile-mavi {")
      && hasCss(".my-game-coming-prototype-rack-tile.tile-sari {")
      && hasCss(".my-game-coming-prototype-rack-tile.tile-siyah {")
      && hasCss(".my-game-coming-prototype-rack-actions {")
      && hasCss(".my-game-coming-prototype-rack-discard-status {")
      && hasCss(".my-game-coming-prototype-meld-draft-status {")
      && hasCss(".my-game-coming-prototype-melds {")
      && hasCss(".my-game-coming-prototype-melds-head {")
      && hasCss(".my-game-coming-prototype-melds-open-summary {")
      && hasCss(".my-game-coming-prototype-melds-list {")
      && hasCss(".my-game-coming-prototype-meld-item {")
      && hasCss(".my-game-coming-prototype-meld-item-tiles {")
      && hasCss(".my-game-coming-prototype-discard {")
      && hasCss(".my-game-coming-prototype-discard-head {")
      && hasCss(".my-game-coming-prototype-discard-status {")
      && hasCss(".my-game-coming-prototype-discard-tiles {")
      && hasCss(".my-game-coming-prototype-discard-empty {")
      && hasCss(".my-game-coming-prototype-log {")
      && hasCss(".my-game-coming-prototype-log-list {")
      && hasCss(".my-game-coming-prototype-log-entry {")
      && hasCss(".my-game-coming-table-sketch-grid {")
      && hasCss(".my-game-coming-table-sketch-empty-state {")
      && hasCss(".my-game-coming-table-sketch-empty {")
      && hasCss(".my-game-coming-table-sketch-empty-reset-btn {")
      && hasCss(".my-game-coming-table-sketch-card.active {")
      && hasCss(".my-game-coming-table-sketch-card.selected {"),
  },
];

const failed = checks.filter((check) => !check.test());

if (failed.length > 0) {
  console.error("Game prototype room sketch smoke FAILED:");
  for (const item of failed) {
    console.error(`- ${item.label}`);
  }
  process.exit(1);
}

console.log("Game prototype room sketch smoke passed.");
